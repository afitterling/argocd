apiVersion: v1
kind: Pod
metadata:
  namespace: teamtwo
  name: nginx
  labels:
    run: myapp
spec:
  selector:
    matchLabels:
      run: myapp
  containers:
  - name: nginx
    image: nginx:1.14.2
    ports:
    - containerPort: 80
