apiVersion: v1
kind: Pod
metadata:
  name: webnode-af
  labels:
    app.kubernetes.io/name: myapp
spec:
  containers:
    - name: nginx
      image: nginx:1.14.2
      ports:
      - containerPort: 80
