apiVersion: v1
kind: Pod
metadata:
  namespace: argocd
  name: webnode
  labels:
    app.kubernetes.io/name: webnode
spec:
  selector:
    matchLabels:
      run: webnode
  containers:
    - name: webnode
      image: nginx:1.14.2
      ports:
      - containerPort: 80
