apiVersion: v1
kind: Pod
metadata:
  namespace: argocd
  name: myapp
  labels:
    name: webapp2
spec:
  selector:
    matchLabels:
      name: webapp2
  containers:
    - name: webapp2
      image: nginx:1.14.2
      ports:
      - containerPort: 80
